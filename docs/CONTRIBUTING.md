# Contributing

Use Node 26, Bun 1.3.14, and Vite+ 0.2.7. Run `vp check` for non-mutating validation and `vp check --fix` to apply fixes. Run targeted tests before the full suite. Keep generated assets consistent with their sources and update canonical documentation contracts with routing changes.
