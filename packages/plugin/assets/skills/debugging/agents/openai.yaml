interface:
  display_name: "Debugging"
  short_description: "Reproduce and repair an observable defect."
  default_prompt: "Reproduce the failure, isolate its cause, make the narrow repair, and prove the regression is gone."
policy:
  allow_implicit_invocation: true
