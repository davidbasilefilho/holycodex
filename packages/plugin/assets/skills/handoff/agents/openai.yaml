interface:
  display_name: "Handoff"
  short_description: "Produce one redacted resumable handoff when work pauses or transfers."
  default_prompt: "Write one redacted handoff containing constraints, evidence, risks, and the next action."
policy:
  allow_implicit_invocation: false
