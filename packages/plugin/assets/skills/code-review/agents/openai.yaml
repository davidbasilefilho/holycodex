interface:
  display_name: "Code review"
  short_description: "Review implementation once to a fixed point."
  default_prompt: "Inspect the actual implementation diff, repair bounded reviewer-owned defects, and return evidence."
policy:
  allow_implicit_invocation: true
