interface:
  display_name: "Context7 CLI"
  short_description: "Find current library and API documentation first."
  default_prompt: "Research the assigned current library or API fact and return sourced evidence with coverage limits."
policy:
  allow_implicit_invocation: true
