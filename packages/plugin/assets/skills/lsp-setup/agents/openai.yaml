interface:
  display_name: "LSP setup"
  short_description: "Configure one required language server with minimum verified setup."
  default_prompt: "Validate the platform and tool, make the minimum setup, and return typed evidence or denial."
policy:
  allow_implicit_invocation: true
