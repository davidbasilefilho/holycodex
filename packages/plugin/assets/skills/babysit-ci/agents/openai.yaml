interface:
  display_name: "Babysit CI"
  short_description: "Observe required CI after an approved origin mutation."
  default_prompt: "Monitor the required CI jobs for the approved push or tag and return terminal evidence."
policy:
  allow_implicit_invocation: false
