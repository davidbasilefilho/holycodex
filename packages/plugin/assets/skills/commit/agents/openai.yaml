interface:
  display_name: "Commit"
  short_description: "Prepare and execute an approved local commit with exact scope proof."
  default_prompt: "Verify the exact scope, obtain approval, create the local commit, and report status."
policy:
  allow_implicit_invocation: false
