# Patterns

Use target-language code shapes. `$A` captures one node, `$$$ARGS` many, `$_` ignores one. Start concrete; vary only needed parts. Quote shell patterns. If a fragment cannot parse alone, use YAML `context` plus `selector`. Verify before rewrite.
