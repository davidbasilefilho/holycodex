# Recipes

- Call: `logger.debug($A)`; any args: `console.log($$$ARGS)`.
- Empty catch: language-correct `catch ($E) {}`.
- Missing await: call plus `not`/`inside` excluding await.
- Import migration: full import/require; preserve captured module/name.

Search narrow path, inspect variants, constrain, review, write, format, test.
