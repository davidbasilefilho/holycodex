# sgconfig

Use `sgconfig.yml` only for reusable project rules. Declare `ruleDirs` and optional test directories using repository-relative paths. Do not add config for one query. Run config-backed scan/test from repository root.
