{
  "name": "@holycodex/workspace",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "generate:codex": "bun scripts/generate-codex-bindings.ts",
    "check": "bun run --parallel generate:codex fmt:check lint",
    "check:fix": "bun run --parallel fmt:write lint:fix",
    "fmt": "oxfmt",
    "fmt:write": "oxfmt --write",
    "fmt:check": "oxfmt --check",
    "lint": "oxlint",
    "lint:fix": "oxlint --fix",
    "build": "bun run --sequential generate:codex bun scripts/package-build.ts",
    "test": "bun run generate:codex && bun test",
    "validate": "bun scripts/validate.ts",
    "version:patch": "bun scripts/version.ts patch",
    "version:minor": "bun scripts/version.ts minor",
    "version:set": "bun scripts/version.ts"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "@types/node": "catalog:",
    "effect": "catalog:",
    "oxfmt": "catalog:",
    "oxlint": "catalog:",
    "oxlint-tsgolint": "catalog:",
    "typescript": "catalog:"
  },
  "packageManager": "bun@1.4.2",
  "catalog": {
    "@opentui/core": "~0.5.11",
    "@toon-format/toon": "^4.1.1",
    "@types/bun": "^1.4.2",
    "@types/node": "^26.5.1",
    "effect": "^3.22.2",
    "oxfmt": "~0.66.0",
    "oxlint": "^1.82.0",
    "oxlint-tsgolint": "^7.0.2001",
    "typescript": "^7.0.2"
  }
}
