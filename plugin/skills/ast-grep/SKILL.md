---
name: ast-grep
description: Structural code search and rewrite with ast-grep.
---

Use `sg` when syntax shape matters. Start with search. Confirm matches. Apply smallest rewrite. Run targeted checks. Never use regex when AST pattern is required.
