# Bun owns project operations; Node runs the npm trusted-publishing boundary.
[tools]
bun = "1.4"
node = "26"
"npm:@openai/codex" = "latest"

[settings]
fetch_remote_versions_cache = "0s"
use_versions_host = false
