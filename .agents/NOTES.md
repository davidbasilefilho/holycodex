# Implementation notes

## Multi-agent compatibility

- Installed Codex reports `multi_agent` as stable and enabled, while `multi_agent_v2` is under development and disabled by default.
- The boolean override `features.multi_agent_v2=true` is accepted by the installed binary; nested community configuration is not treated as a stable public contract.
